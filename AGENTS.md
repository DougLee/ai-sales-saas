# AGENTS.md

## Agent skills

### Issue tracker

Issues 通过 GitHub Issues 跟踪（DougLee/ai-sales-saas），用 `gh` CLI 读写。See `docs/agents/issue-tracker.md`.

### Triage labels

使用默认五标签词汇表（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。See `docs/agents/triage-labels.md`.

### Domain docs

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
